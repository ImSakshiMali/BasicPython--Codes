# iterate over a list of tuples and print each element using a for loop.
list_of_tuples = [(1, 'pen'), (2, 'pencil'), (3, 'marker')]
for tup in list_of_tuples:
    for item in tup:
        print(item)
